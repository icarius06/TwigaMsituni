<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mozilla.hackathon.twigamsituni">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <meta-data android:name="DATABASE" android:value="trafficstats.db" />
        <meta-data android:name="VERSION" android:value="1" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.mozilla.hackathon.twigamsituni.datausage.datausage.persist" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".datausage.TrafficStatActivity"
            android:label="@string/title_activity_traffic_stat"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".datausage.TrafficStatsSummaryActivity"
            android:label="@string/title_activity_traffic_stats_summary"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".datausage.datausage.persist.TrafficRecordUpdateReceiver"
            android:enabled="true"
            android:exported="true"></receiver>
        <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
        <receiver android:name=".datausage.datausage.persist.BootCompleteReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>
